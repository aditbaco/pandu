# Database Configuration
# PostgreSQL database URL for Neon serverless connection
# For Replit: Use provided DATABASE_URL from environment
# For Laragon/Local: postgresql://username:password@localhost:5432/database_name
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# PostgreSQL Connection Details
# For Replit: These are automatically set when using PostgreSQL
# For Laragon/Local: Configure according to your local PostgreSQL setup
PGHOST=localhost
PGPORT=5432
PGUSER=your_username
PGPASSWORD=your_password
PGDATABASE=your_database_name

# Application Configuration
# Environment setting for the application (development/production)
NODE_ENV=development

# Server Configuration
# Port for the Express server
# Replit: Use 5000 (required for proper routing)
# Laragon/Local: Can use 3000, 5000, 8000, etc.
PORT=5000

# Session Configuration (for future authentication features)
# Session secret key for express-session (generate a random string)
SESSION_SECRET=your_random_session_secret_key_here

# Optional: External Service API Keys (if needed in the future)
# Example: EMAIL_API_KEY=your_email_service_api_key
# Example: SMS_API_KEY=your_sms_service_api_key